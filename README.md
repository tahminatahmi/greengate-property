# GreenGate Property

A responsive UK property marketplace built with React, Vite, JavaScript, Tailwind CSS, React Router and Lucide icons.

## Run locally

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The sample property data lives in `src/data/properties.js`.
